#!/bin/bash

ZEUS_DIR=/usr/local/src/zeus

if [ -d $ZEUS_DIR/.git ]
then
	cd $ZEUS_DIR
	ZEUS_VERSION=$(git describe --tags 2>/dev/null)
else
	ZEUS_VERSION=""
fi

echo "{\"olympus-version\":\"$OLYMPUS_VERSION\"}"
